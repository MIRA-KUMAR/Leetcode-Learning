class Solution:
    def findNumbers(self, nums):
        """count = 0
        for n in nums:
            res = 0
            while n > 0:
                tmp = n%10
                res += 1
                n //= 10
            if res% 2== 0:
                count += 1
        return count"""

        count = 0
        for n in nums:
            if len(str(n)) % 2 == 0:
                count += 1
        return count
