def maxSubArray(nums):
    curSub, maxSub = nums[0], nums[0]
    for i in nums[1:]:
        curSub = max(i, curSub+i)
        maxSub = max(maxSub, curSub)
    return maxSub


print(maxSubArray([-2, 1, -3, 4, -1, 2, 1, -5, 4]))
